name: simple
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pkgxdev/dev@v1
      - run: cat $GITHUB_ENV
      - run: env | grep ERL_
      - run: elixir -e "IO.puts(1 + 2)"
