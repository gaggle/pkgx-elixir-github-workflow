name: erl-rootdir-fix
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/elixir-setup-rootdir-fix
      - run: cat $GITHUB_ENV
      - run: elixir -e "IO.puts(1 + 2)"
